//Delivery.js
import React from 'react';
import './Styles.css'

const Delivery = () => {
  return (
    <div className="content-container">
<h2 className='text-centre'>Доставка и оплата</h2>

<p>Доставка:
   Доставка пока не осуществляется</p>

<p>Оплата:</p>
 <p> на карту банка по номеру телефона:</p>
 <p> на карту банка по номеру счета:</p>
 <p> на карту банка по номеру qr-коду:</p>
</div>
  );
}

export default Delivery;